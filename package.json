{
  "name": "vue.js-find-components",
  "version": "0.1.0",
  "description": "Adds global helper function for finding Vue components (window.vueFind = querySelector for VDOM)",
  "scripts": {
    "build": "zip --junk-paths --recurse-paths app.zip app"
  }
}
